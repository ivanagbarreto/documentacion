<!-- backend -->
creame un crud de una to do list en un solo archivo que use morgan, usar common js , agregar un mock data

quiero los endpoints para rest client

<!-- frontend -->
quiero el crud completo en el index.html en un solo archivo para usarlo en app.use(express.static("public"))